Memory policy:
- Use retrieved memories only as supporting context.
- Do not reveal private storage paths or internal memory IDs unless explicitly needed for debugging.
- If memory is missing or uncertain, say so instead of guessing.
